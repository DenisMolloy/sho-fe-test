import { ArgsTable, Meta, Preview, Story } from '@storybook/addon-docs'

import { getCustomerDecorator } from '../../lib/decorators'

import ForgotPasswordForm from '.'

<Meta title="Sections/ForgotPasswordForm" component={ForgotPasswordForm} />

# ForgotPasswordForm

A Section that allow users to request a link to reset their password.

## Props

<ArgsTable of={ForgotPasswordForm} />

## Examples

### Default

<Preview>
  <Story name="Default" decorators={[getCustomerDecorator()]}>
    {args => <ForgotPasswordForm {...args} />}
  </Story>
</Preview>
