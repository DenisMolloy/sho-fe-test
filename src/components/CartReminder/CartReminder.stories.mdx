import { Meta, Preview, Story } from '@storybook/addon-docs'

import CartReminder from '.'

<Meta title="Components/CartReminder" component={CartReminder} />

# CartReminder

The `CartReminder` Component displays the status (i.e. number of items) of the user's cart and displays a dynamic message along with CTA links for the user to interact with.

## Props

This component doesn't rely on props for the data displayed.

### Default

<Preview>
  <Story name="Default">{args => <CartReminder {...args} />}</Story>
</Preview>
